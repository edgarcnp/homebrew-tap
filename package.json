{
  "name": "edgarcnp-homebrew-tap-packaging",
  "private": true,
  "type": "module",
  "description": "Build tooling for the AppImage casks in this tap. TypeScript sources run directly on Node (type stripping); no runtime dependencies.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "fbr": "node packaging/bin/fbr.ts",
    "test": "node --test 'packaging/lib/**/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "lint:shell": "find packaging -name '*.sh' -print0 | xargs -0 shellcheck"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3"
  }
}
